<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
       <variable
           name="postsViewModel"
           type="com.example.postsapp.ui.main.maincomponents.MainViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.main.maincomponents.MainFragment"
        android:background="@drawable/bgic"
        >



        <ProgressBar
            android:id="@+id/postsProgressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="visible"
            app:setVisibleLoading = "@{postsViewModel.navigateToSelectedProperty}"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            />


        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/surahRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"

            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"

            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.0"
            app:layout_constraintStart_toStartOf="parent"
            app:setVisibleListSuccess="@{postsViewModel.navigateToSelectedProperty}"
            tools:listitem="@layout/post_item"

            />


        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="10dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            >

            <TextView
                android:id="@+id/postsErrorMsg"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:visibility="gone"
                android:textSize="20dp"
                android:textColor="@color/red"
                tools:text="Error"
                android:textStyle="bold"
                android:layout_gravity="center"
                android:layout_marginBottom="8dp"
                app:setVisibleError = "@{postsViewModel.navigateToSelectedProperty}"

                />

            <Button
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                 android:backgroundTint="@color/black"
                android:visibility="gone"
                android:layout_gravity="center"
                android:text="@string/retry"
                android:onClick="@{() -> postsViewModel.onRetryButtonClick()}"
                app:setVisibleRetryConnectionBtn = "@{postsViewModel.navigateToSelectedProperty}"

                />


        </LinearLayout>




    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>